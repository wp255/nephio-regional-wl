apiVersion: v1
kind: Service
metadata:
  name: hello
  namespace: edge-demo
spec:
  selector:
    app: hello
  ports:
  - name: http
    port: 80
    targetPort: 5678
    nodePort: 30080
  type: NodePort
